[build]
  base = "apps/landing"
  command = "cd ../.. && npm install && cd apps/landing && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
