[build]
  base = "apps/webdesign"
  command = "cd ../.. && npm install && cd apps/webdesign && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
